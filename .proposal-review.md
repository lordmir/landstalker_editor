# Proposal Review — recreate-decomp-algo

**Generated:** 2026-07-04 10:42:55 UTC
**Model:** claude-opus-4-8
**Effort:** max
**Rubric:** task-proposal.md (6 criteria)

---

Problem statement: The agent is given a Motorola 68000 assembly file at `/app/mapdecompress.asm` that implements a decompression routine for two variable-sized 2D arrays of 16-bit unsigned integers. The agent must reverse-engineer this algorithm and reimplement it in a higher-level language, producing an executable at `/app/decompress` that takes three positional arguments (input binary file, output CSV #1, output CSV #2). Each output CSV is headerless, with every value rendered as a 4-digit zero-padded uppercase hex string, comma-separated, each row (including the last) terminated by `\n`, and no extra whitespace. The first decompressed array goes to the first CSV, the second to the second.

**Verifiable**

Positive: The output is fully deterministic and can be checked by exact byte comparison of the two CSV files against ground truth. The format is rigid enough (fixed hex width, fixed separators, fixed line terminator, no whitespace) that there is no float-tolerance or normalization ambiguity — a diff either matches or it doesn't. The author holds the reference assembly, so they can build an oracle (run the original in a 68000 emulator, or a known-good port) to generate expected outputs for arbitrary inputs. No LLM judge is needed. Re-running the verifier is stable because decompression of a fixed input is deterministic.

Negative: Verification depends on the author having genuine ground truth and on the verifier exercising input binaries the agent can't overfit to. If only a single sample input exists and it's visible, an agent could hardcode. This is a standard implementation concern (use hidden/multiple inputs), not an in-principle problem.

Judgement: Strong Accept

**Well-specified**

Positive: The output contract is unusually precise — hex width, casing, zero-padding, separator, line terminator (including the trailing one), and whitespace prohibition are all pinned down, as are the executable path and argument order. Crucially, the *algorithm* is specified by delegation to a concrete reference artifact (the assembly file), so "what is correct" has exactly one answer: whatever the reference produces. Two reasonable people would both generate their verifier by running the oracle, so their verifiers would agree.

Negative: Prose delegates several structural decisions to the reference that the agent must reverse-engineer: the binary input layout, how array dimensions are encoded, the order of the two arrays, and — most notably — the mapping of a 2D array to CSV rows/columns (row-major vs. transposed). These are knowable from the assembly but are not pinned in the prose, so an agent that misreads the layout will fail. This is inherent to a "recreate this algorithm" task rather than a spec defect, but it is the axis to watch.

Spec-defect patterns observed:
- **Bivalent phrasing**: Mildly present but reference-resolved. "*Write the contents of the first decompressed 2D array to the first CSV file, and the next decompressed 2D array to the second CSV file*" leans on algorithm order to define "first"/"next," and the 2D→CSV orientation admits two readings (row-major vs. transposed). Only one satisfies the verifier, but the disambiguator is the reference assembly the agent is told to replicate, not hidden verifier behavior — so this is difficulty, not a fatal ambiguity.
- **Precondition vs. rejection contract**: None observed. The agent is not asked to validate or reject malformed input.
- **Phantom verification claims**: None observed. The instruction makes no claims about verifier machinery.
- **Phantom requirements**: None observed in wording, but the instruction references "*/app/mapdecompress.asm*" — the author must ensure this file actually exists and contains a working, self-contained algorithm. A commitment for implementation, not a defect in the prose.
- **Domain-term mismatches**: None observed. "CSV," "hex," "16-bit unsigned," and "headerless" are all used consistently with how the verifier would compute them.

Judgement: Accept

**Solvable**

Positive: The author possesses the source, so a correct port is guaranteed to exist and doubles as the oracle. Porting a decompression routine from 68000 assembly to a high-level language, once understood, is a few hours of work — the solution volume is small. Reverse-engineering assembly decompressors is routine in ROM-hacking and preservation communities, so there is ample precedent that this class of problem is tractable.

Negative: Understanding the assembly (addressing modes, sign extension, bit-level ops, loop bounds) could be time-consuming, but that is the difficulty, not a volume problem — the implementation itself stays small. No solvability red flags.

Judgement: Strong Accept

**Difficult**

Positive: This has a strong difficulty profile. It requires reading 68000 assembly (non-obvious domain knowledge), inferring an undocumented binary format, and precisely reproducing bit-level decompression logic. The exact-match output requirement means any off-by-one, endianness slip, or bit-order error is fatal — small mistakes don't degrade gracefully, they fail. That combination of multi-step reasoning, careful state tracking, and cross-domain knowledge is exactly what a competent senior engineer would find non-trivial.

Negative: Difficulty hinges on the complexity of the specific assembly, which isn't visible at proposal stage. A trivial RLE routine could land too easy for a strong agent; an overly baroque one could overshoot. This is the one axis with genuine uncertainty, but the exact-match-on-hidden-input barrier plus the assembly-comprehension barrier make an adequately hard calibration very achievable, and trials will confirm.

Judgement: Accept

**Interesting**

Positive: Reverse-engineering and porting legacy assembly is a real, economically meaningful activity — retro game preservation, fan translation, ROM hacking, decompilation, and migrating code off obsolete hardware. The "mapdecompress" framing points squarely at a game-map decompressor, and there is a well-defined community that does exactly this and would find it compelling.

Negative: None material.

Judgement: Strong Accept

**Outcome-verified**

Positive: Grading is purely on output correctness (the two CSVs). The agent may use any language, tool, or approach — manual disassembly, an emulator, a decompiler — and any method producing byte-correct output passes. The constraints (output path, argument order, CSV format) are legitimate output-shape constraints, not process mandates.

Negative: An agent could conceivably wrap the original assembly in an emulator rather than truly reimplementing it; this is a legitimate path that still yields correct output and requires nontrivial I/O-interface work, so it doesn't undermine verification. Overfitting to a visible sample is the only real risk, addressed by hidden inputs.

Judgement: Strong Accept

**Final analysis**

This is a clean, well-motivated reverse-engineering task with an exceptionally reliable verification story: deterministic decompression plus a rigidly specified output format means byte-exact comparison against an author-held oracle, with no subjectivity and no LLM judge. Solvability is not in question — the author has the source. Interest is clear via the preservation/ROM-hacking community. Outcome verification is sound and method-agnostic. The output contract is tightly specified.

The two areas that require care at implementation time are (1) difficulty calibration, which depends on the complexity of the chosen assembly — the one axis I can't fully assess from the proposal — and (2) ensuring the reference assembly is present and that structural choices the prose delegates to it (array ordering, 2D→CSV orientation) are deterministic and covered by hidden test inputs so an agent can't overfit. Neither is a blocking defect; both sit on a clear, standard path to a passing implementation. Because the difficulty depends on an artifact I can't inspect, I hold at Accept rather than Strong Accept, but the path to acceptance is direct and the task is fundamentally sound.

Decision: Accept
